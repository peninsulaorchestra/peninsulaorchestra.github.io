# UPCOMING concerts. Newest first.
#
# Give an entry a real `date:` (YYYY-MM-DD) and the month/day/year block on the
# left fills itself in. If the date isn't settled, leave `date:` off and set
# `year:` instead — it shows as "Date TBA".
#
# Once a concert has happened, move the entry to _data/past-events.yml.
#
# Nothing is currently announced. Add entries here and the "Upcoming Concerts"
# section replaces its placeholder message automatically.
