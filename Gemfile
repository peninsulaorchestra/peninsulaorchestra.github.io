source "https://rubygems.org"

# Matches the exact stack GitHub Pages runs server-side, so a local
# preview renders the same as the published site.
# See: https://pages.github.com/versions/
gem "github-pages", "~> 232", group: :jekyll_plugins
